meta {
  name: addresses_list
  type: http
  seq: 1
}

get {
  url: {{base_url}}/api/addresses
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

tests {
  test("status is 200", () => {
    expect(res.status).to.equal(200);
  });

  test("returns addresses array", () => {
    expect(res.body.addresses).to.be.an("array");
  });
}
