meta {
  name: settings_public
  type: http
  seq: 1
}

get {
  url: {{base_url}}/api/settings/public
  body: none
  auth: none
}


tests {
  test("status is 200", () => {
    expect(res.status).to.equal(200);
  });
  
  test("returns settings array", () => {
    expect(res.body.settings).to.be.an("array");
  });
}
