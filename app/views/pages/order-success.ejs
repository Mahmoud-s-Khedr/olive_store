<!-- Order Success Page -->
<div class="max-w-[600px] mx-auto px-4 py-16 text-center">

    <!-- Success Animation -->
    <div class="relative w-32 h-32 mx-auto mb-8">
        <div class="absolute inset-0 bg-green-100 rounded-full animate-pulse"></div>
        <div class="relative w-full h-full rounded-full bg-green-500 flex items-center justify-center">
            <span class="material-symbols-outlined text-white text-6xl">check</span>
        </div>
    </div>

    <!-- Success Message -->
    <h1 class="text-3xl md:text-4xl font-bold text-olive-dark mb-4">تم تأكيد طلبك بنجاح!</h1>
    <p class="text-olive-light text-lg mb-8">
        شكراً لك على طلبك. سنقوم بالتواصل معك قريباً لتأكيد التفاصيل.
    </p>

    <!-- Order Details Card -->
    <div id="order-details" class="bg-white rounded-2xl border border-gray-100 p-6 mb-8 text-right">
        <div class="flex items-center justify-between pb-4 border-b border-gray-100 mb-4">
            <span class="text-olive-light">رقم الطلب</span>
            <span id="order-number" class="font-bold text-olive-dark">#---</span>
        </div>

        <div class="space-y-3 text-sm">
            <div class="flex items-center justify-between">
                <span class="text-olive-light">حالة الطلب</span>
                <span
                    class="inline-flex items-center gap-1 px-3 py-1 rounded-full bg-amber-100 text-amber-800 text-xs font-bold">
                    <span class="w-2 h-2 rounded-full bg-amber-500"></span>
                    قيد المعالجة
                </span>
            </div>
            <div class="flex items-center justify-between">
                <span class="text-olive-light">طريقة الدفع</span>
                <span id="payment-method" class="text-olive-dark">الدفع عند الاستلام</span>
            </div>
            <div class="flex items-center justify-between">
                <span class="text-olive-light">تاريخ الطلب</span>
                <span id="order-date" class="text-olive-dark">---</span>
            </div>
        </div>
    </div>

    <!-- What's Next -->
    <div class="bg-primary/5 rounded-2xl p-6 mb-8 text-right">
        <h3 class="font-bold text-olive-dark mb-4 flex items-center gap-2">
            <span class="material-symbols-outlined text-primary">info</span>
            ماذا بعد؟
        </h3>
        <ul class="space-y-3 text-sm text-olive-light">
            <li class="flex items-start gap-2">
                <span class="material-symbols-outlined text-primary text-lg mt-0.5">check_circle</span>
                <span>ستصلك رسالة تأكيد على بريدك الإلكتروني</span>
            </li>
            <li class="flex items-start gap-2">
                <span class="material-symbols-outlined text-primary text-lg mt-0.5">check_circle</span>
                <span>سنتواصل معك لتأكيد موعد التوصيل</span>
            </li>
            <li class="flex items-start gap-2">
                <span class="material-symbols-outlined text-primary text-lg mt-0.5">check_circle</span>
                <span>التوصيل خلال 2-4 أيام عمل</span>
            </li>
        </ul>
    </div>

    <!-- Action Buttons -->
    <div class="flex flex-col sm:flex-row items-center justify-center gap-4">
        <a href="/my-orders"
            class="flex items-center justify-center gap-2 w-full sm:w-auto h-12 px-8 bg-primary hover:bg-primary-dark text-white font-bold rounded-lg transition-all">
            <span class="material-symbols-outlined">shopping_bag</span>
            <span>متابعة طلباتي</span>
        </a>
        <a href="/products"
            class="flex items-center justify-center gap-2 w-full sm:w-auto h-12 px-8 border-2 border-primary text-primary hover:bg-primary hover:text-white font-bold rounded-lg transition-all">
            <span class="material-symbols-outlined">storefront</span>
            <span>متابعة التسوق</span>
        </a>
    </div>

    <!-- Contact Support -->
    <p class="text-olive-light text-sm mt-12">
        هل لديك استفسار؟
        <a href="/contact" class="text-primary font-medium hover:underline">تواصل معنا</a>
        أو اتصل على
        <a href="tel:+201234567890" class="text-primary font-medium hover:underline" dir="ltr">+20 123 456 7890</a>
    </p>
</div>

<script>
    document.addEventListener('DOMContentLoaded', function () {
        // Get order ID from URL
        const urlParams = new URLSearchParams(window.location.search);
        const orderId = urlParams.get('order');

        // Format date in Arabic
        function formatDate(date) {
            const options = { year: 'numeric', month: 'long', day: 'numeric' };
            return new Date(date).toLocaleDateString('ar-EG', options);
        }

        if (orderId) {
            document.getElementById('order-number').textContent = `#${orderId}`;
            document.getElementById('order-date').textContent = formatDate(new Date());

            // TODO: Fetch order details from API if needed
        } else {
            document.getElementById('order-number').textContent = '#' + Math.floor(Math.random() * 90000 + 10000);
            document.getElementById('order-date').textContent = formatDate(new Date());
        }

        // Update cart badge
        const badge = document.getElementById('cart-badge');
        if (badge) {
            badge.classList.add('hidden');
        }
    });
</script>